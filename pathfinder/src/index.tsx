import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';
import { root } from './constants';

root.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);